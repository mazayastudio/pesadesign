# filepath: /c:/Users/mazay/Builds/lulia/Rakefile
require 'jekyll'
require 'jekyll/sass/converter'

task :build do
  Jekyll::Commands::Build.process({})
end

task :serve do
  Jekyll::Commands::Serve.process({})
end